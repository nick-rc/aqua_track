version: "3"

services:
  aqua_track:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./aqua_track:/aqua_track
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
